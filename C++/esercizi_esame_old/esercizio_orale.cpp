#include<iostream>

struct DiskFile{
    DiskFile(string n="",float fs=0.):name(n),fileSize(fs){}
    bool operator==(const DiskFile& right) const{
        if(name==right.name)&&(fileSize)
    }
};
int main(){

}